- name: Build containers and run them
  hosts: localhost
  become: yes
  
  tasks:
    - name: Build app container
      import_role:
        name: build

    - name: Clean up
      import_role:
        name: teardown

    - name: Deploy app
      import_role:
        name: deploy